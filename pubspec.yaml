name: flutter_movies_trending
description: La Haus Technical Test.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Support for other languages
  flutter_localizations:
    sdk: flutter
  # Internationalization and localization facilities
  intl: ^0.17.0
  # Value equality
  equatable: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true

assets:
  - assets/
